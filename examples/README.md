# fresh-fuchs examples

Public-safe example configs, added per phase (model build, scenario,
inner-run, outer-run, pipeline). All examples must use synthetic or
public-safe fixtures — never private or bundled data.
